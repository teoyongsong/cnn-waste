# Byte-compiled / caches
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Environment / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
*.Zone.Identifier
*:Zone.Identifier

# IDE (optional — remove if you commit workspace settings)
.idea/

# Streamlit
.streamlit/secrets.toml

# Large local artifacts (dataset — never commit raw images/zip)
data/

# Model weights: ignored by default (too large / LFS). To commit best_waste_model.pth for Streamlit Cloud, keep the two lines below.
*.pth
!best_waste_model.pth
*.pt
*.ckpt

# Logs / local runs
*.log
.pytest_cache/
.coverage
htmlcov/
